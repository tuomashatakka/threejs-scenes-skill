.DS_Store
node_modules
# Build outputs. public/lib/dist stays COMMITTED so the Pages demos work from a
# raw checkout; skill/lib/dist is regenerated by `bun run build` (and by CI
# before packaging), so it is not tracked. The .skill bundle + its staging dir
# are produced by scripts/package-skill.ts.
/dist
/site-dist
/skill/lib
/threejs-scenes.skill
/threejs-scenes/
.playwright-mcp/
