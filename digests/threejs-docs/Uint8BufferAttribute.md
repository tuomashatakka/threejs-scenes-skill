<!-- ingested from https://raw.githubusercontent.com/mrdoob/three.js/dev/docs/pages/Uint8BufferAttribute.html.md (direct markdown, no model) -->

*Inheritance: BufferAttribute →*

# Uint8BufferAttribute

Convenient class that can be used when creating a `UInt8` buffer attribute with a plain `Array` instance.

## Constructor

### new Uint8BufferAttribute( array : Array.<number> | Uint8Array, itemSize : number, normalized : boolean )

Constructs a new buffer attribute.

**array**

The array holding the attribute data.

**itemSize**

The item size.

**normalized**

Whether the data are normalized or not.

Default is `false`.

## Source

[src/core/BufferAttribute.js](https://github.com/mrdoob/three.js/blob/master/src/core/BufferAttribute.js)
